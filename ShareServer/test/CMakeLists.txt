
#project name :ACSTEST
PROJECT(ACSTEST)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

IF(NOT WIN32)
	ADD_DEFINITIONS("-fprofile-arcs -ftest-coverage")
	LINK_LIBRARIES(gcov)
ENDIF()

ADD_SUBDIRECTORY(ut)
ADD_SUBDIRECTORY(demo)
