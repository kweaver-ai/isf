INCLUDE($ENV{CMAKE}/common/prep.cmake)

PROJECT(ACSPROCESSORUT)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

# 目标名
SET(TARGET_NAME acs_processor_ut)

#
# 目标类型
SET(TARGET_TYPE RUNTIME)

# 依赖库
SET(NEED_XPCOM TRUE)
SET(NEED_CFL TRUE)
SET(NEED_SYSWRAP TRUE)
SET(NEED_NCUTIL TRUE)
SET(NEED_GTEST TRUE)
SET(NEED_GMOCK TRUE)
SET(NEED_EDATAAPI TRUE)
SET(NEED_ETHRIFT TRUE)
SET(NEED_BOOST TRUE)
SET(NEED_BOOST_DATE_TIME TRUE)
SET(NEED_LICENSE TRUE)
SET(ENABLE_C++11 TRUE)
SET(NEED_EHTTPSERVER TRUE)
SET(NEED_EHTTPCLIENT TRUE)
SET(NEED_THRIFTAPI TRUE)
SET(NEED_BRPC TRUE)
SET(NEED_OSSCLIENT TRUE)

# ut 源代码目录
SET(SOURCE_DIRS . $ENV{SHARESERVER}/src/acsprocessor)

SET(ERRGEN_FILES $ENV{SHARESERVER}/src/acsprocessor/err/acsprocessorerr.csv)

# 包含ShareServer
INCLUDE_DIRECTORIES($ENV{SHARESERVER}/src)

ADD_DEFINITIONS("-D__UT__")
ADD_DEFINITIONS("-DHAVE_INTTYPES_H")
ADD_DEFINITIONS("-DHAVE_NETINET_IN_H")

INCLUDE_DIRECTORIES($ENV{DATAENGINEIOC})

INCLUDE($ENV{CMAKE}/common/target.cmake)
